package filehandling;

import javax.xml.parsers.DocumentBuilder;
import javax.xml.parsers.DocumentBuilderFactory;
import org.w3c.dom.Document;
import org.w3c.dom.Element;
import org.w3c.dom.Node;
import org.w3c.dom.NodeList;

public class CountChildElements {

	private static int getChildElementCount(Element element) {
		NodeList childNodes = element.getChildNodes();
		int count = 0;
		for (int i = 0; i < childNodes.getLength(); i++) {
			Node node = childNodes.item(i);
			if (node.getNodeType() == Node.ELEMENT_NODE) {
				count++;
			}
		}
		return count;
	}
	
	public static void main(String[] args) {
		try {
			// Load and parse the XML document
			DocumentBuilderFactory factory = DocumentBuilderFactory.newInstance();
			DocumentBuilder builder = factory.newDocumentBuilder();
			Document document = builder.parse("D:\\InnovationTool\\Demo.xml");

			// Get the root element (Students)
			Element rootElement = document.getDocumentElement();

			System.out.println("Number of child elements under Students: " + getChildElementCount(rootElement));

		} catch (Exception e) {
			e.printStackTrace();
		}
	}
}
